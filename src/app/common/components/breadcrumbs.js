angular.module('app').component('breadcrumbs', {
  templateUrl: 'app/common/templates/breadcrumbs.html',
  controller: breadcrumbsController
});

function breadcrumbsController($scope, $element, $state, $stateParams) {
  var $ctrl = this;
}
